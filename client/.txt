 <button
  type="button"
  onClick={() => {
  setStudentId("");
    setPassword("");
    }}
  className={`flex-1 min-w-[100px] border border-gray-300 py-3 rounded-lg text-lg font-medium cursor-pointer ${
              isLogginIn
                ? "bg-blue-300 text-white cursor-not-allowed border-none"
                : "hover:bg-gray-100 transition duration-200  text-gray-600"
            }`}
          >
    Clear
</button>

// Admin dashboard UI sample
https://www.graciousnaija.com/2021/05/e-voting-system-in-php-mysqli-source.html
https://www.youtube.com/watch?v=aKKcXDi9DK8&t=1035s


// Conventional Commits.

feat: → adding a new feature (e.g., feat: add user profile page)
fix: → fixing a bug (e.g., fix: resolve login redirect issue)
chore: → project maintenance (e.g., chore: update dependencies)
docs: → documentation updates (e.g., docs: add API usage example)
refactor: → code change that isn’t a bug fix or feature (e.g., refactor: simplify login logic)
style: → formatting changes only (no logic change)
test: → adding or fixing tests

  const location = useLocation();
  const pathnames = location.pathname.split("/").filter((x) => x);

  // If the path is just /admin/home, show only "Home"
  if (pathnames.length <= 2) {
    return (
      <nav className="text-sm flex items-center">
        <Link to="/admin/home" className="text-gray-700 hover:text-blue-600">
          {" "}
          <span className="flex items-center gap-2">
            <PieChart />
            Home
          </span>
        </Link>
        <span className="mx-2">›</span>
        <span className="text-gray-400">Dashboard</span>
      </nav>
    );
  }

  // Otherwise show Home › LastSegment
  const lastSegment = pathnames[pathnames.length - 1];

  return (
    <nav className="text-sm flex items-center">
      <Link to="/admin/home" className="text-gray-700 hover:text-blue-600">
        <span className="flex items-center gap-2">
          <PieChart />
          Home
        </span>
      </Link>

      <span className="mx-2">›</span>
      <span className="text-gray-400 capitalize">{lastSegment}</span>
    </nav>
  );